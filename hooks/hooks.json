{
  "description": "Preflight plugin hooks — suggests spec document formats when exiting plan mode in a scaffolded project",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "ExitPlanMode",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/exit-plan-mode.sh"
          }
        ]
      }
    ]
  }
}
