## Review Skill — Section 4.0 Guard Evaluation

**Eval:** Case B — Uninitialized project guard with skill

**Setup:**
- Working directory: $TMPDIR/preflight-uninitialized-review (empty, no .preflight/)
- Target file: any .md file (no file argument provided)

**Sections 1–3 behavior:**
- Section 1: .preflight/config.yml is missing → defaults docs_dir to "docs/"
- Section 2: No argument provided → would glob docs/**/*.md, but no docs/ exists either
- Section 3: Cannot identify doc type without a file to read

**Section 4.0 executed (guard):**

```bash
test -d .preflight/_rules/ && echo "EXISTS" || echo "MISSING"
```

**Result:** MISSING

**Skill response (per SKILL.md section 4.0 guard):**

No `.preflight/_rules/` directory found. Run `/preflight scaffold` first to initialize the project.

**Guard behavior:** PASS — skill correctly detected that .preflight/_rules/ does not exist and halted execution before attempting to load any rules or evaluate the document.

**Notes:**
- The guard in section 4.0 fires even if a valid file was provided as argument — the rules directory check is unconditional
- The guard fires before sections 4.1 and 4.2 (rules file loading)
- The error message correctly directs the user to run `/preflight scaffold` as the remediation step
- This guard protects against silent rule-less "reviews" that would produce vacuously passing results
